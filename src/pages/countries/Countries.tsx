import React from 'react';
import Select from '../../components/select/Select';

const Countries = () => {
  return (
    <div>
          <Select InputProps={[]}/>
    </div>
  );
};

export default Countries;
